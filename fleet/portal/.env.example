# Auth.js
AUTH_SECRET=
AUTH_URL=https://fleet.example.com

# Cognito (public client — no client secret; PKCE)
AUTH_COGNITO_ID=
AUTH_COGNITO_ISSUER=https://cognito-idp.us-east-1.amazonaws.com/<user-pool-id>

# Fleet REST API as seen from the Next.js server process (same EC2: loopback)
FLEET_SERVICE_URL=http://127.0.0.1:8080
