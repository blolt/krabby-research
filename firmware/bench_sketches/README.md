# bench_sketches

Standalone flashable bench tools — not part of the main firmware image. Formal manual bench procedures live in `firmware/bench_tests/` (PR #3); the sketch/test naming split is intentional.
